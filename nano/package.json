{
  "name": "@tiny-calc/nano",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./dist *.build.log",
    "dev": "npm run build -- --watch",
    "test": "ts-node src/nano-calc.ts"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.3"
  }
}
